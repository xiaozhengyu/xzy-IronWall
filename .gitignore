# ---------------------------------------------------------------- 绝不能进仓库
#
# doc/ 里有 GitHub 的账号密码。一旦推上去，即使之后删掉文件，那份凭据在 git 历史里
# 依然可以被翻出来 —— 公开仓库的历史是永久的，改密码是唯一的补救。所以它排在最前面。
doc/

# 其它常见的凭据文件名，防止哪天顺手放了一个进来。
*.pem
*.key
*.p12
*.pfx
.env
.env.*
!.env.example
secrets.*
*密码*
*credentials*

# ---------------------------------------------------------------- 参考素材
#
# 从别处截来的参考图。不进仓库：来源不明的美术素材推到公开仓库有版权风险，而且它们
# 只是画的时候看一眼的对照物，不是项目跑起来需要的资源。
example/

# ---------------------------------------------------------------- 依赖与产物
node_modules
dist
dist-ssr
*.local
.vite

# ---------------------------------------------------------------- 日志
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ---------------------------------------------------------------- 离线预览的产物
#
# `npm run figures` 生成的，都是从代码算出来的，不该进仓库 —— 改一次参数就全变，
# 每次提交都会带一堆二进制 diff。要看效果的人自己跑一次就有。
.preview.mjs
.bench.mjs
.preview-*.png

# 临时的一次性校验脚本（碰撞、烘焙耗时、散布统计之类），跑完即删。
.*check.ts
.*check.mjs

# ---------------------------------------------------------------- 编辑器与系统
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.codex-loop/
