{
  "name": "ironwall",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lan": "vite build && vite preview --host --port 5180",
    "figures": "esbuild tools/preview.ts --bundle --platform=node --format=esm --outfile=.preview.mjs --log-level=warning && node .preview.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "pixi.js": "^8.20.1"
  }
}
